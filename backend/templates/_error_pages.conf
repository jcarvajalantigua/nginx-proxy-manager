{%- if error_pages and error_pages.length > 0 %}
  # Custom Error Pages
{%- for ep in error_pages %}
  error_page {{ ep.code }} /_error_pages/{{ ep.file }};
{%- endfor %}
  location /_error_pages/ {
    internal;
    root /data/nginx;
  }
{%- endif %}
